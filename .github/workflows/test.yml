name: Run tests
on: [push]

jobs:
  tests:
    runs-on: ubuntu-21.04
    container: ppapav/computational-workflows:latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Run tests
        run: |
          py.test-3
